import 'package:flutter/material.dart';

class ListProviderCard extends StatelessWidget {
  const ListProviderCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
